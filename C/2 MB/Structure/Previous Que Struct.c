//Problem
#include<stdio.h>
struct account
{
    int acc_no;
    float balance;
    char name[40];
};
int main()
{
    struct account x,y;
    scanf("%f",&x.balance);
    scanf("%s",y.name);
    printf("%f",x.name);

    return 0;
}
