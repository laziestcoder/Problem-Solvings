/* finding area of a isosceles triangle */
#include <stdio.h>
main (){
float base , height, area;
printf("enter base of the triangle( in cm):");
scanf("%f", &base);
printf("enter height of the triangle (in cm):");
scanf("%f", &height);
area=(base*height)/2;
printf("area of the triangle is : %f square cm", area);}
