#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s, t, t1;
    int n;
    while(cin>>n){
            s = "+x";
        for(int i = 0; i < n-1; i++){
            cin>> t;
            if(t == "+y")

            else if(t == "-y")

            else if(t == "+z")

            else if(t == "-z")

        }
    }
}
