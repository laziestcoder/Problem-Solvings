#include<stdio.h>

int r(int x, int y)
{
    for(i=x,j=y;i<=w,j<=h;)
    {
        if(j==h && i<=w)
            i++;
        else if(i==w && j>=1)
            j--;
        else if(j<=h && i<=w)
        j++;
        else if(i<=h )

    }
}
int main ()
{
    return 0;
}
