#include<stdio.h>
#include<string.h>
int main ()
{
    int t,n,s[1005][12],num[12];
    scanf("%d",&t);
    for(i=1; i<=t; i++)
    {
        scanf("%d",&n);
        for(j=1; j<n ; j++)
        {
            scanf("%d",&s[j]);
        }
        scanf("%d",&num);
        l=strlen(s);
        f=0;
        for(p=0; p<11; p++ )
          {
              if(s[p]!=d[p])
              f++;
          }

    }
    return 0;
}
